services:
  - type: web
    name: klorn-api
    runtime: node
    region: singapore
    plan: free
    buildCommand: |
      corepack enable &&
      pnpm install --frozen-lockfile --prod=false &&
      cd packages/core && pnpm build &&
      cd ../api && npx prisma generate && pnpm build
    startCommand: cd packages/api && pnpm start:prod
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: OPENROUTER_API_KEY
        sync: false
      - key: FRONTEND_URL
        sync: false
      - key: WEB_URL
        sync: false
      - key: GOOGLE_REDIRECT_URI
        sync: false
      - key: PUSH_RECEIPT_BASE_URL
        sync: false
      - key: PORT
        value: "3001"
      - key: CORS_ORIGINS
        value: "https://klorn.ai,https://www.klorn.ai"
      - key: RENDER_EXTERNAL_URL
        value: "https://klorn-api.onrender.com"
      - key: BETA_AUTO_PRO_ENABLED
        sync: false
      - key: BETA_AUTO_PRO_LIMIT
        sync: false
