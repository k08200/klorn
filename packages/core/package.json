{
  "name": "@klorn/core-shim",
  "version": "0.0.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "description": "No-op build shim for Render's stale buildCommand — see README.md. NOT the old @klorn/core (removed in #821).",
  "scripts": {
    "build": "echo '@klorn/core-shim: no-op (Render stale buildCommand shim, see packages/core/README.md)'"
  }
}
